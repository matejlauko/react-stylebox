import { type UserTheme } from './types';

const defaultTheme: UserTheme = {
  typographySetup: {
    baseFontSize: 1,
    fontSizeScale: 'step2',
    lineHeight: 1.3,
  },
};

export default defaultTheme;
